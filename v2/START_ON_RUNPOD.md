# ⚡ Start NexusLang v2 on RunPod - ULTRA SIMPLE

**Get your platform running in 5 commands**

---

## 🎯 THE ENTIRE PROCESS

### On RunPod.io:
1. Deploy CPU Pod (30GB disk)
2. Expose ports: **3000, 8000**
3. Click "Connect" → "Web Terminal"

### In the Terminal, paste these 5 commands:

```bash
# 1. Install Git
apt-get update && apt-get install -y git

# 2. Clone project
cd /workspace && git clone https://github.com/your-org/project-nexus.git && cd project-nexus

# 3. Run deployment
chmod +x v2/deploy-to-runpod.sh && ./v2/deploy-to-runpod.sh

# 4. Wait 3 minutes for services to start...

# 5. Check status
docker-compose ps
```

### Get Your URL:
- **In RunPod dashboard** → Your pod → Find "TCP Port Mappings"
- Port **3000** → That's your frontend URL!
- Open it in browser → **YOU'RE LIVE!** 🚀

---

## ✅ THAT'S IT!

**Time:** 5 minutes  
**Cost:** ~$0.30/hour (stop when not using)  
**Result:** Live NexusLang v2 platform accessible worldwide!

---

## 🎊 What You Get:

- ✅ Web IDE at port 3000
- ✅ Backend API at port 8000
- ✅ All 12 examples working
- ✅ Binary compilation
- ✅ Personality editor
- ✅ Full documentation

---

## 📞 Quick Commands:

```bash
# View logs
docker-compose logs -f

# Restart
docker-compose restart

# Stop (save money)
docker-compose down

# Start again
docker-compose up -d
```

---

## 🚀 SHARE WITH USERS:

```
🎉 Try NexusLang v2 Alpha!

http://your-runpod-url:3000/ide

Features:
⚡ 10x faster AI processing
🧠 Personality system
📚 Knowledge queries
🎤 Voice commands

Free to use!
```

---

**GO TO RUNPOD.IO NOW!** 🚀

